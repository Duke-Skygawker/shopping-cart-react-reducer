import { CLEAR_LIST, RESTE_LIST, REMOVE_ITEM } from "./actions";

const reducer = (state, action) => {
  if (action.type) {
    return;
  }
  if (action.type) {
    return;
  }
  if (action.type) {
    return;
  }
  throw new Error(`No matching "$action.type" - action type`);
};

export default reducer;
